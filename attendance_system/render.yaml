services:
  - type: web
    name: ece-attendance-system
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir attendance_system app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: ATTENDANCE_STORAGE_DIR
        value: /tmp
